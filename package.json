{
  "name": "imagesystems",
  "private": true,
  "engines": {
    "node": ">=20.19.0"
  },
  "workspaces": [
    "frontend"
  ],
  "scripts": {
    "postinstall": "patch-package",
    "setup:backend": "cd backend && python3 -m venv .venv && ./.venv/bin/python -m pip install -U pip && ./.venv/bin/pip install -r requirements.txt",
    "check-backend": "node scripts/check-backend-venv.mjs",
    "backend": "node scripts/run-backend.mjs",
    "dev": "npm run check-backend && concurrently -n api,web -c blue,magenta \"LOCAL_DEV_MODE=true LOCAL_DEV_SKIP_UPSCALE=true npm run backend\" \"npm run dev --prefix frontend\"",
    "dev:full": "npm run check-backend && concurrently -n api,web -c blue,magenta \"LOCAL_DEV_MODE=true LOCAL_DEV_SKIP_UPSCALE=true npm run backend\" \"VITE_VERBOSE_API_LOGS=true npm run dev --prefix frontend\"",
    "dev:local": "npm run dev:full",
    "dev:supabase": "concurrently -n api,web -c blue,magenta \"npm run backend\" \"VITE_LOCAL_DEV_MODE=false VITE_STORAGE_ONLY=false VITE_USE_API=true VITE_VERBOSE_API_LOGS=true npm run dev --prefix frontend\"",
    "dev:web": "node scripts/warn-frontend-only.mjs && npm run dev --prefix frontend",
    "dev:all": "npm run dev",
    "build": "npm run build -w frontend",
    "lint": "npm run lint -w frontend",
    "preview": "npm run preview -w frontend",
    "install-frontend": "npm install",
    "electron:dev": "cross-env ELECTRON_DEV=1 electron electron/main.cjs",
    "electron:dev:wait": "cross-env ELECTRON_DEV=1 wait-on http://127.0.0.1:2020 && electron electron/main.cjs"
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "cross-env": "^7.0.3",
    "electron": "^33.4.0",
    "patch-package": "^8.0.1",
    "wait-on": "^8.0.1"
  }
}
